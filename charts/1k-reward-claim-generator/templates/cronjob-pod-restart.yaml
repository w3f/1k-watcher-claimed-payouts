apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ include "1k-reward-claim-generator.name" . }}
  labels:
    app: {{ include "1k-reward-claim-generator.name" . }}
spec:
  schedule: {{ .Values.schedule | quote }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: {{ include "1k-reward-claim-generator.name" . }}
        spec:
          containers:
          - name: {{ include "1k-reward-claim-generator.name" . }}
            image: {{ .Values.image.repo }}:{{ .Values.image.tag }}
            imagePullPolicy: IfNotPresent
            env:
            resources:
{{ toYaml .Values.resources | indent 16 }}
            {{- end }}
          restartPolicy: Never